
-content_for :top_bar_title do
  ='Склады'

-content_for :top_bar_links do
  =link_to 'Создать', new_warehouse_path, class: 'button'

-content_for :top_bar_search do
  =render 'shared/search', data: {path: warehouses_path, placeholder: 'Номер склада'}

.main-content
  .row
    -@warehouses.each do |warehouse|
      =render 'shared/form', object: warehouse
